{
  "name": "root",
  "private": true,
  "main": "src/plugin/index.js",
  "type": "module",
  "scripts": {
    "test": "node ./test/parser-test.js && node ./test/prettier-plugin-test.js",
    "write": "prettier --plugin=./ --write $npm_config_path --ignore-path .prettierignore --no-error-on-unmatched-pattern --loglevel=warn"
  },
  "dependencies": {
    "yaml": "^2.2.2",
    "antlr4": "^4.13.0"
  },
  "devDependencies": {
    "@types/node": "^18.16.3",
    "prettier": "^3.0.0"
  }
}
