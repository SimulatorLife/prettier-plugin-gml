name: Codex 15 – Parameter Flexibility
on:
  repository_dispatch:
    types: [run_scheduled]
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write
  issues: write

jobs:
  run:
    if: ${{ (github.event_name == 'workflow_dispatch' || (github.event_name == 'repository_dispatch' && github.event.action == 'run_scheduled' && github.event.client_payload.workflow_file == '.github/workflows/codex-15-parameters.yml')) }}
    uses: ./.github/workflows/_codex-open-pr-and-ping.yml
    secrets: inherit
    with:
      pr_title: "Codex: Parameter Flexibility"
      pr_body: "Seed PR for Codex to refactor a rigid constant into a configurable parameter."
      prompt: >
        Search for hardcoded constants or magic numbers that could instead be configuration/parameters.
        Refactor one meaningful value into a configurable parameter. Integrate it
        cleanly with existing systems to reduce rigidity and make the codebase more flexible.
