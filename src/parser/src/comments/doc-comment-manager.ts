// This file intentionally removed - prefer `@gml-modules/core` direct usage.
// Consumers should directly import from Core, such as:
// import { Core } from "@gml-modules/core";
// const docManager = Core.prepareDocCommentEnvironment(ast);
export {};
