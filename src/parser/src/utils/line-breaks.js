// Re-export line break helpers from @gml-modules/core to keep parser behavior
// aligned with the shared implementation.
export { getLineBreakSpans, getLineBreakCount, splitLines } from "@gml-modules/core";
