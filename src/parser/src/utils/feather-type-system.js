// Minimal stub for the feather type system utilities.
// This file intentionally provides a small export so linters that forbid
// empty files (unicorn/no-empty-file) are satisfied. Implementations live
// elsewhere or will be added when the feature expands.

export const FeatherTypeSystem = Object.freeze({});

export default FeatherTypeSystem;
