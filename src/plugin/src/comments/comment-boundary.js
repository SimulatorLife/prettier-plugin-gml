import { Core } from "@gml-modules/core";
const { AST: { collectCommentNodes, getCommentArray, hasComment, isBlockComment, isCommentNode, isDocCommentLine, isLineComment } } = Core;
export { collectCommentNodes, getCommentArray, hasComment, isBlockComment, isCommentNode, isDocCommentLine, isLineComment };

import { Core } from "@gml-modules/core";
const { Utils: { getLineBreakCount, splitLines, isObjectLike } } = Core;
export { getLineBreakCount, splitLines, isObjectLike };

