var squared = value * value;
var withParens = (value) * (value);
var member = player.velocity.x * player.velocity.x;
var index = coords[0] * coords[0];
var unchangedCall = update() * update();
var commented = value /* keep */ * value;
