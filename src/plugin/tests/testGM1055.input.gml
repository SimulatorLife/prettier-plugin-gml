function example(_foo, _bar = 0, _baz) {
    var sum = argument0 + argument1;

    function inner(_value, _other) {
        return argument0 + argument1;
    }

    return argument2 + sum + argument0;
}
