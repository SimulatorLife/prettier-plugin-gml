var missing
var intact = 1;
if (scr_custom_eval())
{
    var inside
    var withComment // comment

    inside = 2;
    show_debug_message(inside);
    withComment = 3;
    show_debug_message(withComment);
}
