import { Core } from "@gml-modules/core";
const {
    Resources: {
        GML_IDENTIFIER_METADATA_PATH,
        GML_IDENTIFIER_METADATA_URL,
        loadBundledIdentifierMetadata
    }
} = Core;
export {
    GML_IDENTIFIER_METADATA_PATH,
    GML_IDENTIFIER_METADATA_URL,
    loadBundledIdentifierMetadata
};

import { Core } from "@gml-modules/core";
const {
    Resources: { FEATHER_METADATA_PATH, loadBundledFeatherMetadata }
} = Core;
export { FEATHER_METADATA_PATH, loadBundledFeatherMetadata };
