player = { hp: 100, mp: 50 };

global.AsyncLoaderQueue = ds_queue_create();
global.AsyncLoaderHandle = -1;

player2[slot].hp = 100;
player2[slot].mp = 50;

function make_stats() {
    var stats = { hp: 100, mp: 50 };
    return stats;
}
