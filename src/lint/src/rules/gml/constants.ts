/**
 * Shared constants for GML lint rules and transforms.
 */

/** Minimum number of consecutive variable declarations before blank-line spacing is enforced. */
export const DEFAULT_VARIABLE_BLOCK_SPACING_MIN_DECLARATIONS = 4;
