/**
 * Minimum consecutive variable declarations before a blank line is enforced before a following `for` loop.
 * Mirrors the same constant in the plugin workspace to keep both transforms in sync.
 */
export const DEFAULT_VARIABLE_BLOCK_SPACING_MIN_DECLARATIONS = 4;
